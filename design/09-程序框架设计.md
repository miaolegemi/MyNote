# 09 — 程序框架设计

## 设计目标

定义游戏的技术架构、核心类的继承体系和模块划分。本文档衔接设计文档与代码实现，为开发团队提供可直接落地的技术蓝图。

**定位：** 定于"怎么做"而非"做什么"——每个系统明确定义其 UE5 基类、继承链、关键接口和数据契约。

---

## 技术选型

| 层面 | 选型 | 理由 |
|------|------|------|
| 引擎 | **Unreal Engine 5** | 大型世界流式加载、程序化地形、C++ + Blueprint 双栈 |
| 玩法逻辑 | UE C++（核心系统）+ **Blueprint**（玩法编排） | C++ 实现性能关键路径，Blueprint 编排跨系统流程 |
| 伤害/效果 | **Gameplay Ability System (GAS)** | 角色伤害、环境伤害、状态效果、Buff/Debuff 统一使用 GAS 的 AttributeSet + GameplayEffect + GameplayAbility |
| 程序化生成 | UE PCG 框架 + **自定义规则引擎** | PCG 处理地形/植被；规则引擎驱动 POI 模板组合 |
| 物理 | UE Chaos Physics | 飞船飞行、零重力环境、碰撞检测 |
| 网络 | 单机优先，后期扩展 UE Replication | 模块边界预留网络同步接口（GAS 原生支持网络同步） |
| UI | UE Common UI + UMG | 跨平台输入适配 |
| 音频 | UE MetaSounds + Wwise | 程序化音频（引擎声随参数变化） |
| 存档 | 自定义序列化（二进制 + JSON 元数据） | 二进制存运行时状态，JSON 存元数据便于调试 |

**Build.cs 依赖模块（公有）：** `Core`, `CoreUObject`, `Engine`, `InputCore`, `EnhancedInput`, `UMG`, `StructUtils`, `GameplayAbilities`, `GameplayTags`, `GameplayTasks`
**Build.cs 依赖模块（私有）：** `Slate`, `SlateCore`

---

## UE5 游戏框架注册（与代码一致）

```
UGameInstance
 └── ULoneStarGameInstance           // 持有全局单例 Subsystem 引用
      ├── UGameDirector              // (GameInstanceSubsystem) 已实现：游戏全局流程控制
      └── UUIManager                 // (GameInstanceSubsystem) 已实现：UI 状态管理

// 以下为设计阶段规划，尚未在代码中实现：
//    ├── UBuildManager              // (GameInstanceSubsystem) 规划中
//    ├── UUniverseManager           // (GameInstanceSubsystem) 规划中
//    ├── UProgressionManager        // (GameInstanceSubsystem) 规划中
//    ├── UResourceEconomyManager    // (GameInstanceSubsystem) 规划中
//    ├── UNarrativeManager          // (GameInstanceSubsystem) 规划中
//    └── UEventBus                  // (GameInstanceSubsystem) 规划中

AGameMode
 └── ALsrGameMode                     // 已实现：玩家重生逻辑（RestartPlayer 重写）

APawn
 ├── ALsrCharacter : ACharacter, IAbilitySystemInterface  // 已实现：玩家角色（第三人称），拥有 UAbilitySystemComponent、UVitalAttributeSet、UInteractionComponent
 └── ALsrShipPawn                     // 已实现：飞船载具 Pawn，含 SpringArm + Camera + FloatingPawnMovement

APlayerController
 └── ALsrPlayerController             // 已实现：玩家控制器
```
### Subsystem 选型原则

| 生命周期 | 基类 | 适用系统 | 原因 |
|----------|------|----------|------|
| 跨关卡持久 | `UGameInstanceSubsystem` | 建造、宇宙、进度、资源、叙事、事件总线 | 状态不随加载/卸载而丢失 |
| 单关卡/世界 | `UWorldSubsystem` | 生存、战斗、探索、POI | 随世界加载初始化，随世界销毁清理 |

---

## 关键类继承关系总览

```
UObject
 ├── UGameInstanceSubsystem           // 全局管理器基类
 │    ├── UGameDirector               // 已实现：游戏全局流程控制
 │    ├── UUIManager                  // 已实现：UI 状态（如打开/关闭背包）
 │    ├── UKeyManager                 // 已实现：按键管理器（创建并持有 UInputAction + UInputMappingContext）
 │    └── USaveManager                // 已实现：存档管理器（多槽位存档、OnSave/OnLoad 委托）
 │    // 规划中: UBuildManager, UUniverseManager, UProgressionManager,
 │    //          UResourceEconomyManager, UNarrativeManager, UEventBus
 │
 ├── UWorldSubsystem                  // 世界级管理器基类（规划中）
 │    // USurvivalManager, UCombatManager, UExplorationManager, UPOIManager
 │
 ├── UInventoryComponent              // 已实现：库存容器组件（UActorComponent）
 │    ├── TArray<UItemInstance*> Items
 │    ├── int Capacity
 │    ├── bool AddItem(int ItemId, int Num)
 │    └── bool ExchangeItem(UInventoryComponent* Other, int FromIdx, int ToIdx)
 │
 ├── UItemInstance                    // 已实现：物品运行时实例
 │    ├── int64 ID                    // 物品模板 ID
 │    ├── int64 Num                   // 堆叠数量
 │    └── static CreateItem(...)      // 工厂方法
 │
 ├── USaveData                        // 已实现：存档数据对象（USaveGame）
 │    ├── FPlayerSaveData PlayerData   // 玩家位置/旋转/生命/氧气
 │    ├── TArray<FItemSaveData> InventoryItems
 │    ├── FDateTime SaveTimestamp
 │    └── FString SlotName
 │
 ├── UDataAsset                        // 所有配置数据的基类（规划中）
 │    ├── UModuleDef                  // 模块定义
 │    ├── UFacilityDef                // 舱内设施定义 {FIntVector Volume, EFacilitySurface, bAllowStacking}
 │    ├── URecipeDef                  // 制作配方
 │    ├── UTechNodeDef                // 科技树节点
 │    └── UPOITemplate                // 兴趣点模板
 │
 ├── UBuildValidator                   // 建造验证器基类（规划中）
 │    ├── UShipBuildValidator         // 飞船验证
 │    └── UBaseBuildValidator         // 基地验证
 │
 ├── UConduitDef (UDataAsset)           // 管线定义
 │    ├── EConduitType Type              //   主电力/次级电力/氧气/水/燃料/数据
 │    ├── float MaxCapacity              //   最大传输容量
 │    └── UMaterialInterface* VisualMesh //   暴露/保护两种材质
 │
 ├── UAITask                           // AI 行为任务基类（规划中）
 │    // UAITask_TerritoryDefense, UAITask_PirateRaid, UAITask_PredatorHunt, ...

 └── UUserWidget                       // UI 基类
      ├── UMainMenu                  // 骨架（头文件存在，类声明待补）
      ├── UInventoryWidget           // 骨架
      └── UItemWidget                // 骨架
// ── 接口 (Interfaces) ──
UInterface
 └── IInteractableInterface               // 已实现：可交互接口
      ├── bool OnInteract(AActor* Instigator)       // 交互触发
      ├── FText GetInteractPrompt() const           // 交互提示
      └── bool IsInteractable() const                // 是否可交互

// ── GAS: 属性集 (Attributes) ──
UAttributeSet
 ├── UVitalAttributeSet                 // 已实现：一级生存指标
 │    ├── FGameplayAttributeData Health       // 0-100
 │    ├── FGameplayAttributeData MaxHealth    // 最大生命
 │    ├── FGameplayAttributeData Oxygen       // 0-100
 │    └── FGameplayAttributeData MaxOxygen    // 最大氧气
 ├── UConditionAttributeSet             // 二级生存指标
 │    ├── FGameplayAttributeData Food         // 0-100
 │    ├── FGameplayAttributeData Water        // 0-100
 │    ├── FGameplayAttributeData BodyTemp     // 摄氏度
 │    └── FGameplayAttributeData Radiation    // 累积值
 ├── UShipAttributeSet                  // 飞船属性
 │    ├── FGameplayAttributeData HullIntegrity  // 船体完整性
 │    ├── FGameplayAttributeData ShieldPower   // 护盾值
 │    ├── FGameplayAttributeData PowerOutput   // 当前可用电力
 │    └── FGameplayAttributeData MassTotal     // 总质量
 └── UFacilityAttributeSet              // 设施属性
      └── FGameplayAttributeData Durability    // 耐久度

// ── GAS: 能力 (Abilities) ──
UGameplayAbility
 ├── UGA_UseMedkit                      // 使用医疗包（恢复 Health）
 ├── UGA_RepairModule                   // 修理模块（恢复 Durability）
 ├── UGA_FireWeapon                     // 开火（施加 Damage Effect 到目标）
 ├── UGA_ActivateShield                 // 激活护盾
 ├── UGA_OverloadEngine                 // 引擎过载（临时加速，之后引擎受损）
 └── UGA_EmergencyEscape                // 紧急脱离（瞬发曲速，偏离目的地）
AActor
 ├── AApplianceActor                   // 已实现：电力器具基类（飞船舱内设备）
 │    ├── FElectricNode* InNode        //   输入电力节点
 │    ├── FElectricNode* InNode2       //   第二输入电力节点
 │    ├── FElectricNode* OutNode       //   输出电力节点
 │    ├── FElectricNode* OutNode2      //   第二输出电力节点
 │    ├── AApplianceActor* ParentAppliance  //   链表：父器具
 │    ├── AApplianceActor* ChildAppliance   //   链表：子器具
 │    ├── AApplianceActor* TempTail         //   尾部追踪临时指针
 │    ├── float ConsumePower                //   电力消耗
 │    ├── virtual void IniElectricNode()    //   初始化电力节点
 │    ├── void Linked()                     //   链接到电力链
 │    ├── virtual AApplianceActor* GetTail()
 │    ├── virtual void TailChanged(AApplianceActor* NewTail)
 │    └── void SetNoCableLink(FElectricNode* TargetNode) // 无线直连
 │
 ├── ALsrCharacter : ACharacter, IAbilitySystemInterface // 已实现：玩家角色
 │
 ├── ALsrShipPawn                      // 已实现：飞船载具（第三人称驾驶）
 │
 ├── ABuildableActor                   // 已实现：所有可建造模块的基类
 │    ├── USceneComponent* ModuleRoot  //   场景根组件
 │    └── UStaticMeshComponent* MeshComponent  //   静态网格体
 │    // 规划中子类: AShipModuleActor, ABaseModuleActor
 │
 ├── AInteriorFacilityActor            // 已实现：舱内设施基类
 │    ├── USceneComponent* FacilityRoot       //   设施根组件
 │    └── UStaticMeshComponent* MeshComponent //   静态网格体
 │    // 已实现子类: AApplianceActor
 │    // 规划中子类: ACraftingStationActor, AStorageLockerActor, ...
 │
 ├── ACelestialBodyActor               // 规划中：天体基类
 │    // APlanetActor, AStarActor, AAsteroidFieldActor
 │
 ├── APOIActor                         // 规划中：兴趣点基类
 │    // AAbandonedStationPOI, AAlienRuinPOI, APirateOutpostPOI
 │
 ├── ADoorActor                          // 规划中：舱门
 │    // 类型: Standard/Reinforced/Airtight/Emergency/Maintenance
 │
 ├── AInfrastructureActor                // 规划中：舱壁管线
 │    // 类型: MainPower/SecondaryPower/Oxygen/Water/Fuel/Data
 │
 └── AProjectileActor                  // 规划中：投射物基类
      // ALaserProjectile, AMissileProjectile, ATorpedoProjectile

ACharacter
 ├── ALsrCharacter                      // 已实现：玩家角色
 └── AEnemyPawn                          // 规划中：敌方步行角色
      // APirateCrewPawn, ASecurityDronePawn, AAlienCreaturePawn

UActorComponent
 ├── UAbilitySystemComponent            // GAS 核心：挂载到所有需伤害/效果的 Actor
 │    ├── TArray<UAttributeSet*> AttributeSets   // 属性集
 │    ├── TArray<UGameplayAbility*> Abilities    // 已授予的能力
 │    └── TArray<FActiveGameplayEffect> ActiveEffects  // 当前生效的效果
 │
 ├── UInteractionComponent              // 已实现：交互检测组件（视锥检测）
 │    ├── float InteractRange           //   检测范围（300）
 │    ├── float InteractAngle           //   检测角度（45°）
 │    ├── TScriptInterface<IInteractableInterface> CurrentInteractable  //   当前交互目标
 │    ├── void PerformDetection()       //   每帧执行视锥检测
 │    └── void Interact()               //   触发交互
 │
 ├── UInteractableComponent : IInteractableInterface  // 已实现：可交互组件
 │    ├── FText InteractionPrompt       //   交互提示文本
 │    ├── bool bEnabled                 //   是否启用
 │    └── FOnInteract OnInteractDelegate //   交互事件委托
 │
 ├── UInventoryComponent               // 已实现：库存容器（UActorComponent）
 │    ├── UReactorComponent            // 反应堆（发电）
 │    ├── UEngineComponent             // 引擎（推力 + 机动）
 │    ├── ULifeSupportComponent        // 生命支持（制氧/温控）
 │    ├── UWeaponHardpointComponent    // 武器硬点
 │    ├── UShieldComponent             // 护盾（吸收/再生/过载）
 │    ├── UCargoHoldComponent          // 货舱（容量 + 负重）
 │    ├── UScannerComponent            // 扫描仪
 │    ├── UDockingComponent            // 对接器
 │    ├── ULandingGearComponent        // 起落架
 │    └── UGravDriveComponent          // 重力推动器（FTL）
 │
 ├── UVitalSignComponent               // 生存指标（O2/HP/食物/水/温度/辐射）
 ├── UInventoryComponent               // 库存管理
 ├── UCharacterEquipmentComponent       // 角色装备（挂载 AExplorerPawn）
 │    └── UArmorComponent               // 护甲（伤害减免）
 ├── UDoorComponent                     // 舱门（连接两个舱室）
 │    ├── EDoorType DoorType            // Standard/Reinforced/Airtight/Emergency/Maintenance
 │    └── bool CanConnect(UDoorComponent* Other)  // 类型匹配检查
 ├── UInfrastructureComponent           // 舱壁管线（按类型挂载到墙面/地板/天花板）
 │    ├── EConduitType Type              // MainPower/SecondaryPower/Oxygen/Water/Fuel/Data
 │    ├── TArray<FConduitSegment> Segments  // 线路段（起点→终点，可分支）
 │    └── float CurrentLoad / MaxCapacity  // 当前负载/最大容量
 ├── UPowerConsumerComponent             // 耗电接口（所有需供电的对象都挂载此组件）
 │    ├── float PowerDraw               // 耗电量
 │    └── bool bIsPowered               // 是否正常供电
 // ── 设施功能组件（挂载到 AInteriorFacilityActor 子类） ──
 ├── UCraftingComponent                  // 制造逻辑（配方查找 + 材料校验 + 队列）
 ├── UHealingComponent                   // 治疗逻辑（伤病诊断 + 治疗执行）
 ├── URestComponent                      // 休息逻辑（恢复疲劳 + 清除疲劳状态效果）
 ├── UAnalysisComponent                  // 分析逻辑（扫描数据处理 + 产出科学点）
 ├── URespawnComponent                   // 重生逻辑（死亡时选择休眠仓 + 克隆生成）
 ├── UUpgradeComponent                   // 升级逻辑（Mark I/II/III + 升级材料消耗）
 ├── UWeaponComponent                   // 武器行为基类
 │    ├── ULaserWeaponComponent        // 激光炮（舰载）
 │    ├── UMassDriverWeaponComponent   // 电磁炮（舰载）
 │    ├── UMissileLauncherComponent    // 导弹架（舰载）
 │    ├── UIonCannonComponent          // 离子炮（舰载）
 │    ├── UTorpedoLauncherComponent    // 鱼雷发射器（舰载）
 │    └── UHandheldWeaponComponent     // 手持武器（登舰/遗迹战斗）
 │         ├── URifleComponent         // 步枪
 │         ├── UPistolComponent        // 手枪
 │         ├── UShotgunComponent       // 霰弹枪
 │         └── UCutterToolComponent    // 切割工具（近战）
 │
 ├── UAIBrainComponent                  // AI 大脑
 │    ├── UPirateBrainComponent        // 海盗 AI
 │    ├── UCreatureBrainComponent      // 宇宙生物 AI
 │    └── UBossBrainComponent          // Boss AI
 │
 ├── UBuildPreviewComponent             // 建造预览（半透明吸附）
 └── USignalEmitterComponent            // 信号发射（传感器侦测用）

USceneComponent
 ├── UModuleMeshComponent               // 模块可视化网格（LOD 管理）
 ├── UThrusterVFXComponent              // 引擎粒子/特效
 └── UCockpitCameraComponent            // 驾驶舱视角
```

---

## 核心系统详细设计

### 系统交互拓扑

```
                    ┌─────────────────┐
                    │    UEventBus     │
                    │ (全局事件中枢)    │
                    └────────┬────────┘
                             │
      ┌──────────────────────┼──────────────────────┐
      │          │           │           │          │
┌─────▼───┐ ┌───▼────┐ ┌───▼─────┐ ┌───▼────┐ ┌───▼─────┐
│UBuild   │ │USurvival│ │UUniverse│ │UResource│ │UCombat  │
│Manager  │ │Manager  │ │Manager  │ │Economy  │ │Manager  │
│(GI)     │ │(World)  │ │(GI)     │ │Mgr (GI) │ │(World)  │
└────┬────┘ └───┬─────┘ └────┬────┘ └───┬─────┘ └───┬─────┘
     │          │            │          │           │
     │     ┌────▼─────┐ ┌───▼──────┐ ┌─▼──────┐ ┌─▼──────────┐
     │     │UProgress │ │UExplore │ │UCraft │ │UNarrative  │
     │     │ionManager│ │ationMgr │ │System  │ │Manager (GI)│
     │     │(GI)      │ │(World)  │ │(in Res │ │            │
     │     └──────────┘ └─────────┘ │Econ)   │ └────────────┘
     │                              └────────┘
     └── 建造是核心枢纽，与其他所有系统交互
         (GI) = GameInstanceSubsystem  (World) = WorldSubsystem
```

---

### 1. 建造系统

```
UObject
 └── UGameInstanceSubsystem
      └── UBuildManager                    // 建造总控
           ├── 持有 TArray<UShipRuntime*>   // 所有飞船
           ├── 持有 TArray<UBaseRuntime*>   // 所有基地
           ├── UBuildUndoManager* UndoMgr   // 撤销/回退
           └── USchematicManager* SchematicMgr  // 设计图管理

// ── 建造器 ──
UObject
 ├── UBuilderBase                          // 建造器基类
 │    ├── UModulePlacer* Placer           // 模块放置（网格吸附/旋转/预览）
 │    └── UBuildValidator* Validator      // 建造验证器
 │
 ├── UShipBuilder : UBuilderBase          // 飞船建造器
 │    ├── UFlightChecker* FlightCheck     // 飞行检查（硬性错误/软性警告）
 │    └── UInteriorLayout* Interior       // 内部布局（门/梯子手动放置）
 │
 └── UBaseBuilder : UBuilderBase          // 基地建造器
      ├── UFoundationCheck* Foundation    // 地基/地形检测
      └── ULifeSupportValidator* LSValidator  // 生命支持负担验证

// ── 建造验证 ──
UObject
 └── UBuildValidator
      ├── virtual Validate(const FBuildSnapshot&) → FValidationReport
      │
      ├── UShipBuildValidator : UBuildValidator
      │    ├── CheckRequiredModules()
      │    ├── CheckPowerBudget()
      │    ├── CheckMassThrustRatio()
      │    ├── CheckLandingGearLoad()
      │    ├── CheckDockingPosition()
      │    ├── CheckConnectivity()         // BFS 验证连通路径
      │    └── CheckSizeLimit()
      │
      └── UBaseBuildValidator : UBuildValidator
           ├── CheckLifeSupportLoad()
           ├── CheckFoundation()
           └── CheckEnvironmentReinforcement()

// ── 模块实例（世界中的 Actor） ──
AActor
 └── ABuildableActor
      ├── FModuleInstanceData InstanceData // 运行时数据
      ├── UModuleMeshComponent* MeshComp   // 渲染
      ├── TArray<UModuleComponent*> Components // 功能组件
      │
      ├── AShipModuleActor : ABuildableActor  // 飞船上的模块
      └── ABaseModuleActor : ABuildableActor  // 基地上的模块

// ── 模块功能组件 ──
UActorComponent
 └── UModuleComponent                      // 所有模块功能的基类
      ├── virtual void OnPowerStateChanged(EPowerState NewState)
      ├── virtual float GetPowerDraw()
      └── FAbilityValue Provides          // 此模块提供的能力
```

**关键接口：**
```cpp
// UBuildManager
UFUNCTION(BlueprintCallable)
FPlacementResult PlaceModule(
    UModuleDef* ModuleDef, 
    FIntVector GridPosition, 
    ERotation Rotation);
    
FValidationReport ValidateShip(UShipRuntime* Ship);
FValidationReport ValidateBase(UBaseRuntime* Base);

// UBuilderBase
void PushUndoSnapshot();
void PopUndoSnapshot();
void EnterBlueprintMode();    // 先规划再建造
void CommitBlueprint();

// USchematicManager
USchematicData* SaveSchematic(const FBuildSnapshot& Layout);
FBuildSnapshot LoadSchematic(USchematicData* Schematic);
```

**运行时数据结构：**
```cpp
USTRUCT()
struct FBuildSnapshot {
    TArray<FModuleInstanceData> ModuleList;
    FConnectionGraph ConnectionGraph;      // 模块连接关系（邻接表）
    FPowerBudget PowerBudget;              // 电力使用/上限
    float MassTotal;
    FInteriorGraph InteriorGraph;          // 内部通道连通图
};

USTRUCT()
struct FModuleInstanceData {
    FGuid InstanceID;
    UModuleDef* ModuleDef;
    FIntVector GridPosition;
    ERotation Rotation;
    float Durability;
};

USTRUCT()
struct FValidationReport {
    TArray<FValidationError> Errors;       // 硬性错误：必须修复
    TArray<FValidationWarning> Warnings;   // 软性警告：可忽略
    bool bIsValid;
};
```

---

### 2. 生存系统

```
UObject
 └── UWorldSubsystem
      └── USurvivalManager                 // 低频 Tick (1Hz)，管理所有实体的生存状态
           ├── UVitalSignMonitor* VitalMonitor
           ├── UConditionMonitor* ConditionMonitor
           ├── UInjurySystem* InjurySystem
           ├── UStatusEffectSystem* EffectSystem
           └── UHullBreachSystem* BreachSystem  // 船体破损/减压管理

// ── 生存指标组件（挂载在 Pawn 上） ──
UActorComponent
 └── UVitalSignComponent
      ├── FVitalSigns Current             // 当前所有指标
      ├── FVitalSignRates Rates            // 消耗/补充速率
      └── void TickVitalSigns(float DeltaTime)

// ── 状态效果 ──
UObject
 └── UStatusEffect
      ├── FText EffectName
      ├── float Duration
      ├── virtual void OnApplied(UVitalSignComponent* Target)
      ├── virtual void OnTick(float DeltaTime)
      └── virtual void OnRemoved()
     
      // 子类实现具体效果
      ├── UHypothermiaEffect               // 移速-20%、操作延迟
      ├── UHyperthermiaEffect              // 视野模糊、生命值缓降
      ├── URadiationSicknessEffect         // 生命上限降低（I/II/III 三级）
      ├── UHypoxiaEffect                   // 视野收窄、操作精度下降
      ├── UDecompressionEffect             // 听力模糊、视野震荡（30s 恢复）
      └── UFatigueEffect                   // 操作速度-15%

// ── 伤病 ──
UObject
 └── UInjury
      ├── EInjurySeverity Severity          // Minor/Moderate/Severe/Permanent
      ├── virtual void ApplyEffects(UVitalSignComponent* Target)
      └── virtual void Heal(UHealingItem* Item)

// ── 船体破损与减压 ──
UObject
 └── UHullBreachSystem
      ├── void OnModuleBreached(ABuildableActor* Module)
      │    // 舱壁破裂 → 该舱室急速失压 → O2 极速流失
      └── void SealCompartment(ABuildableActor* Module)
           // 关闭防水门隔离受损舱段
```

**数据流：**
```
每 1 秒:
    USurvivalManager::Tick(DeltaTime)
        → for each UVitalSignComponent in World:
            → 基于当前环境计算消耗率
                ├── IsInSealedEnvironment？ → 低消耗
                ├── IsInBreachedCompartment？ → 高消耗（急速失压）
                └── IsInHazardZone？ → 额外消耗（辐射/温度/有毒大气）
            → 更新各项指标
            → 检查阈值
                ├── O2 < 30% → FireEvent("O2Warning")
                ├── O2 < 10% → FireEvent("O2Critical")
                ├── Health <= 0 → FireEvent("PlayerDeath")
                └── Radiation > Threshold → ApplyStatusEffect(URadiationSicknessEffect)
```

---

### 3. 宇宙管理系统

```
UObject
 └── UGameInstanceSubsystem
      └── UUniverseManager
           ├── int32 UniverseSeed           // 全局种子（存档一致）
           ├── TArray<UStarSystemData*> Systems   // 20-30 个星系数据
           ├── UNavigationSystem* Navigation
           └── USystemGenerator* Generator   // 程序化生成

// ── 星系数据（非 Actor，纯数据对象） ──
UObject
 └── UStarSystemData
      ├── FGuid SystemID
      ├── int32 SystemSeed                    // 星系种子 = UniverseSeed + SystemCoordHash
      ├── FStarData PrimaryStar
      ├── TArray<UPlanetData*> Planets
      ├── TArray<UAsteroidBeltData*> Belts
      ├── TArray<UPOIData*> PointsOfInterest
      └── UDeepSpaceZoneData* DeepSpace

// ── 天体 Actor（世界中的可视化实体） ──
AActor
 └── ACelestialBodyActor
      ├── UCelestialBodyData* BodyData       // 天体数据
      ├── UProceduralMeshComponent* Surface  // 程序化表面
      └── UResourceDistributionComponent* Resources // 资源分布
      
      ├── APlanetActor : ACelestialBodyActor
      │    ├── UAtmosphereComponent* Atmosphere
      │    └── UPCGTerrainComponent* Terrain  // UE PCG 驱动
      │
      ├── AStarActor : ACelestialBodyActor
      │    └── UStarLightComponent* Light
      │
      └── AAsteroidFieldActor : ACelestialBodyActor
           └── TArray<AAsteroidActor*> Asteroids

// ── 程序化生成 ──
UObject
 └── USystemGenerator
      ├── UStarSystemData* GenerateSystem(int32 Seed)
      ├── void PopulateCelestialBodies(UStarSystemData* System)
      ├── void DistributeResources(UStarSystemData* System)    // 基于物理规则
      ├── void PlacePOITemplates(UStarSystemData* System)      // 模板投放
      └── void GenerateBiomes(APlanetActor* Planet)

// ── 航行 ──
UObject
 └── UNavigationSystem
      ├── FTravelRoute PlanRoute(FGuid FromSystem, FGuid ToSystem)
      ├── float CalculateFuelCost(const FTravelRoute& Route)
      ├── TArray<FTravelEvent> GenerateTravelEvents(const FTravelRoute& Route)
      └── ETravelResult ExecuteTravel(AShipPawn* Ship, const FTravelRoute& Route)

// ── 流式加载 ──
UObject
 └── UCelestialLODManager
      ├── void UpdateLOD(FVector PlayerPosition)
      │    // 近距 (<100km) → 全细节: 物理 + 碰撞 + AI
      │    // 中距 (100km-1AU) → 简化模型，无碰撞
      │    // 远距 (>1AU) → 天体图标/光点
      │    // 跨星系 → 仅星图数据
      └── TArray<FCelestialLODState> ActiveStates
```

---

### 4. 资源经济系统

```
UObject
 └── UGameInstanceSubsystem
      └── UResourceEconomyManager
           ├── UInventoryManager* InventoryMgr
           ├── UCraftingSystem* CraftingSys
           ├── UProcessingSystem* ProcessingSys
           ├── URecyclingSystem* RecyclingSys
           └── UAutomationSystem* AutomationSys   // 后期解锁

// ── 库存 ──
UActorComponent
 └── UInventoryComponent                    // 挂载到 Pawn / 飞船 / 基地 / 容器
      ├── TArray<FItemStack> Items
      ├── float MaxWeight
      ├── float CurrentWeight
      ├── bool AddItem(UItemDef* Item, int32 Count) → FAddResult
      ├── bool RemoveItem(UItemDef* Item, int32 Count)
      └── bool CanFit(UItemDef* Item, int32 Count)

// ── 制作 ──
UObject
 └── UCraftingSystem
      ├── TArray<FRecipeDef*> Recipes
      ├── FCraftingResult CraftItem(URecipeDef* Recipe, UInventoryComponent* SourceInventory)
      ├── bool CanCraft(URecipeDef* Recipe, UInventoryComponent* SourceInventory)
      └── FCraftingQueue Queue              // 大型物品有建造进度

// ── 加工设施（基座上的模块组件） ──
UActorComponent
 └── UProcessingFacilityComponent
      ├── URecipeDef* CurrentRecipe
      ├── float Progress
      ├── void StartProcessing(URecipeDef* Recipe)
      └── void TickProcessing(float DeltaTime)

// ── 回收 ──
UObject
 └── URecyclingSystem
      ├── float GetRecycleEfficiency(UProcessingFacilityComponent* Facility)  // 50%-80%
      ├── TArray<FItemStack> RecycleItem(UItemDef* Item)
      └── TArray<FItemStack> RecycleModule(ABuildableActor* Module)  // 建造拆除走全额返还
```

**数据定义（DataAsset）：**
```cpp
UCLASS(BlueprintType)
class UItemDef : public UDataAsset {
    FName ItemID;
    EItemTier Tier;              // 1-4
    int32 MaxStackSize;
    float UnitMass;
    EItemCategory Category;      // Consumable/Material/Component/Equipment
    URecipeDef* Recipe;          // 如果可制造
};

UCLASS(BlueprintType)
class URecipeDef : public UDataAsset {
    FName RecipeID;
    TMap<UItemDef*, int32> InputMaterials;
    TMap<UItemDef*, int32> OutputItems;
    float CraftTime;             // 秒（0=即时）
    UProcessingFacilityComponent* RequiredFacility;
};
```

---

### 5. 战斗系统

```
UObject
 └── UWorldSubsystem
      └── UCombatManager
           ├── UShipCombatSystem* ShipCombat
           ├── UBoardingCombatSystem* BoardingCombat  // 登舰与舱内战斗
           ├── UStealthSystem* Stealth
           └── UAIBrainManager* AIManager

// ── 舰船战斗 ──
UObject
 └── UShipCombatSystem
      ├── UTargetingSystem* Targeting
      │    ├── void LockTarget(AActor* Target)
      │    ├── void SelectSubsystem(ESubsystemType Type)
      │    │    // Engine / Weapon / Shield / Reactor
      │    └── float GetLockStrength(AActor* Target)
      │
      ├── UDamageResolver* DamageResolver
      │    ├── FDamageResult ResolveDamage(const FWeaponHit& Hit)
      │    │    // 流程: 弹道飞行 → 点防御拦截检测 → 护盾吸收
      │    │    //       → 船体装甲减伤 → 模块耐久扣除 → 系统故障概率
      │    └── void ApplyDamageToModule(ABuildableActor* Module, float Damage)
      │
      ├── UShieldSimulator* ShieldSim
      │    ├── float AbsorbDamage(float IncomingDamage)
      │    ├── void Regenerate(float DeltaTime)
      │    └── void Overload()          // 瞬间满额吸收但冷却更长
      │
      └── UPowerTactical* PowerTactical
           ├── void AllocatePower(ESubsystemType Target, float Amount)
           └── void AutoBalance()       // AI 使用的自动分配策略

// ── 武器组件 ──
UActorComponent
 └── UWeaponComponent
      ├── float FireRate
      ├── float PowerDraw
      ├── virtual void Fire(AActor* Target)
      └── virtual bool CanFire()
      
      ├── ULaserWeaponComponent         // 精确、即时命中、能耗高
      ├── UMassDriverWeaponComponent    // 穿透护盾、弹道延迟、弹药有限
      ├── UMissileLauncherComponent     // 追踪、可被拦截、弹药昂贵
      ├── UIonCannonComponent           // 瘫痪系统、短射程
      └── UTorpedoLauncherComponent     // 极高伤害、极慢、可拦截

// ── AI 大脑 ──
UActorComponent
 └── UAIBrainComponent
      ├── UAITask* CurrentTask
      ├── virtual UAITask* EvaluateNextTask()   // 基于当前状态选择行为
      └── TArray<UAITask*> BehaviorTree
      
      ├── UPirateBrainComponent
      │    // 行为优先级: 劫掠评估 → 攻击/索要货物 → 受伤撤退
      │
      ├── UCreatureBrainComponent
      │    // 行为优先级: 领地防御 / 猎食追踪 → 回避强敌 → 致伤撤退
      │
      └── UBossBrainComponent
           // 多阶段行为、特殊技能触发条件、弱点暴露周期

// ── AI 行为任务 ──
UObject
 └── UAITask
      ├── virtual void OnStart(AAIController* Controller)
      ├── virtual void OnTick(float DeltaTime)
      └── virtual ETaskResult Evaluate()
      
      ├── UAITask_TerritoryDefense       // 仅在领地内攻击，不追击远离领地的玩家
      ├── UAITask_PirateRaid             // 评估目标价值 vs 自身损伤，不合算就撤退
      ├── UAITask_PredatorHunt           // 追踪弱小信号，避开强大信号
      ├── UAITask_Revenge                // 派系好感度低于阈值后追杀
      └── UAITask_FearRetreat           // 受重伤后撤退，呼叫增援

// ── 登舰战斗 ──
UObject
 └── UBoardingCombatSystem
      ├── bool CanBoard(AShipPawn* TargetShip)
      │    // 条件: 目标引擎已瘫痪 + 在对接范围内
      ├── void InitiateBoarding(AShipPawn* TargetShip)
      │    // 对接 → 生成 AExplorerPawn → 切换到第三人称行走
      ├── TArray<AEnemyPawn*> SpawnDefenders(ABuildableActor* ShipInterior)
      │    // 在敌船内部根据其舱室布局生成防守船员
      └── FBoardingResult ResolveBoarding()
           // 玩家存活 + 敌船员全灭 = 成功占领
           // 可掠夺货舱物资、拖回基地拆解或自用

// ── 角色装备组件 ──
UActorComponent
 └── UCharacterEquipmentComponent         // 挂载在 AExplorerPawn 上
      ├── UWeaponComponent* PrimaryWeapon  // 步枪/手枪/霰弹枪
      ├── UWeaponComponent* MeleeTool      // 切割工具（近战）
      ├── UArmorComponent* Armor           // 护甲（影响伤害减免）
      └── int32 AmmoReserve[]              // 弹药按类型分开计数，稀缺资源
```

---

### 6. 进度系统

```
UObject
 └── UGameInstanceSubsystem
      └── UProgressionManager
           ├── UTechTreeManager* TechTree
           ├── USchematicManager* Schematics
           │    ├── TArray<USchematicData*> ResearchSchematic    // 研究解锁
           │    ├── TArray<USchematicData*> DiscoveredSchematic  // 野外发现
           │    └── TMap<FGuid, int32> SchematicFragments       // 碎片拼合进度
           │
           ├── UMilestoneTracker* Milestones
           │    ├── TArray<EMilestoneID> ReachedMilestones
           │    └── void CheckMilestone(EMilestoneID ID)        // 建造/探索事件触发
           │
           └── UBuildProgressionLink* BuildProgLink
                ├── void OnModulePlaced(UModuleDef* Module)     // 监听建造事件
                └── TMap<UModuleDef*, EAbility> ProgressionNodes  // 建造驱动的能力

// ── 科技树 ──
UObject
 └── UTechTreeManager
      ├── TArray<UTechNodeDef*> AllNodes
      ├── TArray<UTechNodeDef*> UnlockedNodes
      ├── float SciencePoints
      ├── bool CanResearch(UTechNodeDef* Node) {
      │    // 条件: 科学点足够？前置科技完成？材料样本到位？
      │    }
      └── void Research(UTechNodeDef* Node) {
           // 扣科学点 → 解锁节点 → 解锁关联设计图 → FireEvent("TechUnlocked")
           }

// ── 科技节点定义 ──
UCLASS(BlueprintType)
class UTechNodeDef : public UDataAsset {
    FName NodeID;
    ETechDomain Domain;              // Energy/Propulsion/Materials/Survival/Weapons/Detection
    int32 ScienceCost;
    TArray<UTechNodeDef*> Prerequisites;
    TArray<UItemDef*> RequiredSamples;     // 特定材料样本
    TArray<USchematicData*> UnlocksSchematic;  // 解锁的设计图
    FString SpecialCondition;         // 如"被辐射伤害过"才能研究抗辐射科技
};
```

---

### 7. 探索与发现系统

```
UObject
 └── UWorldSubsystem
      └── UExplorationManager
           ├── UScannerSystem* Scanner
           │    ├── UPassiveScanner* Passive         // 进入星系自动获取
           │    ├── UActiveScanner* Active           // 扫描脉冲
           │    ├── UCloseRangeScanner* CloseRange   // 详细扫描
           │    └── UOnFootInvestigator* OnFoot      // 登陆/进入设施实地勘察
           │
           ├── USignalDetector* SignalDetector
           │    ├── TArray<FSignalSource> ActiveSignals
           │    └── FSignalSource DetectSignal(FVector Location, float Range)
           │
           ├── UStarmapManager* Starmap
           │    ├── FStarmapState MapState            // 已探索/未探索
           │    ├── TArray<FPlayerMarker> Markers     // 自定义标记
           │    └── URoutePlanner* RoutePlanner       // 航线规划
           │
           ├── UDiscoveryTracker* DiscoveryTracker
           │    └── TArray<FDiscoveryRecord> Discoveries // 首次发现 → 命名权
           │
           └── UStarmapDataMarket* DataMarket
                └── float EvaluateDataValue(FDiscoveryRecord Record)  // 星图数据交易

// ── 扫描接口 ──
UObject
 └── UScannerSystem
      ├── FScanResult PassiveScan(APlanetActor* Planet)    // 恒星数、行星数
      ├── FScanResult ActivePulse(UStarSystemData* System)  // 资源分布、信号源
      ├── FScanResult CloseRangeScan(ACelestialBodyActor* Body)  // 矿物含量、生命迹象
      ├── FScanResult OnFootInvestigate(APOIActor* POI)    // 进入设施，详细勘察
      └── void OnScanCompleted(FScanResult Result) {
           // FireEvent("ScanCompleted", Result)
           // → ProgressionManager 获得科学点数
           // → NarrativeSystem 获得线索碎片
           }

// ── 序列化友好定义 ──
UENUM()
enum class EScanLevel : uint8 {
    Passive,        // 进入星系
    Active,         // 扫描脉冲
    CloseRange,     // 飞近目标
    OnFoot          // 登陆/进入设施实地勘察
};

USTRUCT()
struct FScanResult {
    EScanLevel Level;
    ACelestialBodyActor* Target;
    TMap<UItemDef*, float> DiscoveredResources;
    TArray<FSignalSource> DetectedSignals;
    TArray<USchematicData*> BlueprintFragments;
    float SciencePointsEarned;
};
```

---

### 8. 叙事系统

```
UObject
 └── UGameInstanceSubsystem
      └── UNarrativeManager
           ├── UClueNetworkManager* ClueNetwork
           │    ├── TArray<FClueFragment> CollectedClues  // 已收集的线索碎片
           │    ├── TArray<FNarrativeArc> ActiveArcs       // 当前激活的叙事弧
           │    └── FPlayerKnowledge PlayerKnowledge       // 玩家已知信息集合
           │
           ├── UFactionManager* Factions
           │    ├── TMap<UFactionConfig*, float> Reputations  // -100 到 +100
           │    ├── float GetTradeModifier(UFactionConfig* Faction)  // 好感度→价格
           │    └── bool IsHostile(UFactionConfig* Faction)          // 是否主动攻击
           │
           ├── UEnvironmentalNarrative* EnvNarrative
           │    ├── void SpawnDataLog(APOIActor* POI, FDataLogEntry Entry)
           │    ├── void SpawnAudioLog(APOIActor* POI, FAudioLogEntry Entry)
           │    └── void ApplyVisualLayout(APOIActor* POI, FLayoutHint Hint)
           │         // 残骸方向、尸体位置暗示发生了什么
           │
           └── UNarrativeBranchManager* BranchManager
                └── bool EvaluateBranch(FBranchCondition Condition)
                     // 基于玩家行为触发叙事分支

// ── 派系配置 ──
UCLASS(BlueprintType)
class UFactionConfig : public UDataAsset {
    FName FactionID;
    FText DisplayName;
    EFactionIdeology Ideology;          // 秩序/自由/利润/孤立/狂热/...
    FLinearColor ThemeColor;
    TArray<FReputationThreshold> Thresholds;  // 好感度阈值 → 行为变化
};
```

---

## 事件总线

系统间唯一合法的跨系统通信方式。

```cpp
UCLASS()
class UEventBus : public UGameInstanceSubsystem {
public:
    DECLARE_DYNAMIC_MULTICAST_DELEGATE_OneParam(FOnModulePlaced, const FModulePlacedEvent&, Event);
    DECLARE_DYNAMIC_MULTICAST_DELEGATE_OneParam(FOnO2Critical, const FO2CriticalEvent&, Event);
    DECLARE_DYNAMIC_MULTICAST_DELEGATE_OneParam(FOnTechUnlocked, const FTechUnlockedEvent&, Event);
    // ... 其余事件
    
    void Subscribe(FName EventName, UObject* Subscriber, FName HandlerFunc);
    void Unsubscribe(FName EventName, UObject* Subscriber);
    void Publish(FName EventName, const FEventData& Data);
    
private:
    TMap<FName, FOrderedHandlerList> HandlerMap;  // 按优先级排序
    TQueue<FDeferredEvent> DeferredQueue;          // 异步延迟事件
};
```

### 核心事件

| 事件名 | 发送方 | 接收方 | 关键数据 |
|--------|--------|--------|----------|
| `ModulePlaced` | UBuildManager | Progression, Survival | ShipID, ModuleDef, Position |
| `ModuleRemoved` | UBuildManager | Survival, ResourceEconomy | ShipID, ModuleID, RefundMaterials |
| `O2Critical` | USurvivalManager | HUD, Combat | PawnID, O2Percent |
| `ShipDestroyed` | UCombatManager | Progression, Narrative, Survival | ShipID, DestroyerID, Location |
| `PlayerDied` | USurvivalManager | Save, Progression | DeathContext |
| `TechUnlocked` | UProgressionManager | Build, ResourceEconomy | TechID, NewSchematics[] |
| `SystemDiscovered` | UExplorationManager | Narrative, Progression | SystemID, DiscoveryData |
| `ScanCompleted` | UScannerSystem | Progression, ResourceEconomy | Target, DiscoveredData |
| `ReputationChanged` | UFactionManager | Combat, ResourceEconomy | FactionID, OldValue, NewValue |
| `BuildValidationFailed` | UBuildValidator | HUD | ShipID, Errors[], Warnings[] |
| `ProgressionNodeReached` | UProgressionManager | Narrative, HUD | NodeID, UnlockedAbility |

---

## 已实现系统详情

### 电力网络系统（已实现）

采用**普通 C++ 类**（非 UObject），通过链表和信号标志实现灵活的电力传输与逻辑控制。

```
ElectricManager                          // 管理器（非 UObject，手动管理生命周期）
 ├── TSet<FElectricNode*> ElectricNodes  // 所有节点集合
 ├── FElectricNode* AddNode(...)         // 创建并注册节点
 ├── void RemoveNode(FElectricNode*)     // 移除节点
 └── void Tick()                         // 每帧驱动: Transfer() → PrepareNextPeriod()

FElectricNode                            // 电力网络节点（普通 C++ struct）
 ├── float InputPower                    // 本帧输入功率
 ├── float MaxInputPower                 // 过载保护阈值（默认 9999）
 ├── float Capacity / MaxCapacity        // 电池（缓存多余功率）
 ├── float ConsumePower                  // 自身消耗
 ├── ELESignalFlag SignalFlag            // 输出信号标志
 ├── ELESignalFlag WorkFlag              // 当前工作信号
 ├── FElectricNode* LinkedNode           // 下游节点（单向链表）
 ├── void Transfer()                     // 传输逻辑: 信号判断 → 过载检测 → 电池充放 → 传递
 └── void PrepareNextPeriod()            // 周期重置: InputPower → BeforeInput, 清除标志
```

**信号标志 (ELESignalFlag)** — 布尔逻辑路由：
```
None | NoContains | Not | Or1 | Or2 | And1 | And2 | Xor1 | Xor2
```
- 使用 `ENUM_CLASS_FLAGS` 支持位运算
- `Transfer()` 中通过 `EnumHasAllFlags`/`EnumHasAnyFlags` 判断节点是否通电
- 信号随功率沿网络传播：上游输出 → `|=` 写入下游 `WorkFlag`

**器具链表 (AApplianceActor)**：继承自 `AInteriorFacilityActor`
```
AApplianceActor : AInteriorFacilityActor
 ├── InNode / InNode2 / OutNode / OutNode2  // 最多 4 个电力节点
 ├── ParentAppliance / ChildAppliance        // 链表结构（单数命名）
 ├── TempTail                         // 尾部追踪临时指针
 ├── float ConsumePower               // 电力消耗
 ├── virtual void IniElectricNode()   // 子类重写：创建专属电力节点
 ├── virtual AApplianceActor* GetTail()
 ├── virtual void TailChanged(AApplianceActor* NewTail)
 ├── void Linked()                    // 链接到电力链
 └── void SetNoCableLink(FElectricNode* TargetNode)  // 无线直连
```

**设计要点：**
- 非 UObject 设计适合高频 Tick（避免 GC 开销），但需注意**手动内存管理**——`AddNode` 用 `new` 分配，目前无对应的 `delete`
- 信号标志系统让电力网络支持"条件通电"——如"仅当引擎启动且武器未过热时，护盾增强器才通电"
- 链表结构支持器具串联：电源 → 反应堆 → 配电板 → 各设备

### 物品与库存系统（已实现）

采用**片段模式 (Fragment Pattern)**——组合优于继承，类似 UE Lyra 的物品系统。

**数据定义层（USTRUCT，编译期静态数据）：**

```
FTableRowBase
 └── FItemTableRow                         // DataTable 行（策划配置）
      ├── int ID                           // 唯一标识
      ├── int MaxNum                       // 最大堆叠数
      ├── int Durability                   // 耐久度
      └── TArray<FInstancedStruct> Fragments  // 片段数组
           // 元数据: BaseStruct=FItemFragment, ExcludeBaseStruct
           // 可选子类: FItemFragment_Display, (未来扩展: _Equippable, _Usable, ...)

FItemFragment (USTRUCT, BlueprintType)
 └── FItemFragment_Display                 // 显示片段
      ├── FString Name                     // 物品名
      └── FString Desc                     // 物品说明
```

**运行时实例层（UObject，运行时状态）：**

```
UActorComponent
 ├── UInventoryComponent                   // 库存容器组件（挂载到 Pawn / 飞船 / 容器 Actor）
 │    ├── int Capacity                     // 最大槽位数
 │    ├── TArray<UItemInstance*> Items     // 物品实例数组
 │    ├── bool AddItem(int ItemId, int Num)  // 添加（自动堆叠）
 │    ├── UItemInstance* RemoveItemAt(int Idx) // 移除
 │    ├── bool ExchangeItem(...)           // 跨组件交换/合并
 │    └── int GetItemNum(int ItemId)       // 统计某物品总数
 │
 └── UItemInstance                         // 物品运行时实例
      ├── int64 ID                         // 模板 ID（对应 FItemTableRow::ID）
      ├── int64 Num                        // 堆叠数量
      ├── TSubclassOf<FItemTableRow> DefData  // 注意：TSubclassOf 用于 USTRUCT 可能有问题
      └── static CreateItem(UInventoryComponent*, int Index, int ItemId, int Num)  // 工厂方法
```

**已实现：** 添加/移除/交换/统计 | **未实现：** 同ID堆叠、物品使用效果、数据查找（`GetItemData` 返回 nullptr）

### 交互系统（已实现）

基于视锥检测的通用交互系统。通过接口 + 组件的模式分离检测与行为。

**接口与组件：**
```
UInterface
 └── IInteractableInterface                   // 接口
      ├── bool OnInteract(AActor* Instigator)      // 触发交互
      ├── FText GetInteractPrompt() const          // 获取提示文本
      └── bool IsInteractable() const              // 是否可交互

UActorComponent
 ├── UInteractableComponent : IInteractableInterface  // 可交互组件
 │    ├── FText InteractionPrompt                       // 提示文本
 │    ├── bool bEnabled                                 // 启停开关
 │    └── FOnInteract OnInteractDelegate                // 交互事件广播
 │
 └── UInteractionComponent                              // 交互检测组件
      ├── float InteractRange = 300.0f                  // 检测范围
      ├── float InteractAngle = 45.0f                   // 检测角度（度）
      ├── TScriptInterface<IInteractableInterface> CurrentInteractable  // 当前目标
      ├── void BindInput(UInputComponent*)              // 绑定交互输入
      ├── void Interact()                               // 触发当前目标交互
      └── void PerformDetection()                       // 每帧视锥检测
```

**检测流程：**
```
每帧 TickComponent:
  PerformDetection()
    → GetPlayerView()                  // 获取玩家视角位置与方向
    → LineTraceSingleByChannel()       // 从视角中心向前射线检测
    → 命中 Actor + 目标有 IInteractableInterface？
         → 更新 CurrentInteractable
         → Broadcast OnInteractableChanged
    → 无命中或目标不可交互？
         → CurrentInteractable = nullptr
```

### 按键管理器（已实现）

```
UGameInstanceSubsystem
 └── UKeyManager                           // 按键管理器
      ├── UInputAction* InteractAction     // E — 交互
      ├── UInputAction* MoveForwardAction  // W/S — 前后移动
      ├── UInputAction* MoveRightAction    // A/D — 左右移动
      ├── UInputAction* LookAction         // 鼠标 — 视角
      ├── UInputAction* JumpAction         // Space — 跳跃
      ├── UInputMappingContext* DefaultMappingContext
      ├── void ApplyMappingContext(APlayerController*, int32 Priority)
      └── void RemoveMappingContext(APlayerController*)
```

输入动作和映射上下文由 `UKeyManager` 在 `Initialize` 时创建。角色/飞船通过 `ApplyMappingContext` 注册输入。详见 [[11-按键控制与UI功能]]。

### 存档管理器（已实现）

```
UGameInstanceSubsystem
 └── USaveManager                          // 存档管理器
      ├── USaveData* CurrentSaveData       // 当前存档数据
      ├── void SaveToSlot(FString SlotName)   // 保存到指定槽位
      ├── bool LoadFromSlot(FString SlotName) // 从指定槽位读取
      ├── bool DoesSlotExist(FString SlotName) // 检查存档是否存在
      ├── void DeleteSlot(FString SlotName)   // 删除存档
      ├── void AutoSave()                     // 自动存档
      ├── FOnSaveGame OnSave                // 保存委托（各系统绑定）
      └── FOnLoadGame OnLoad                // 读取委托（各系统绑定）

USaveGame
 └── USaveData                            // 存档数据对象
      ├── FPlayerSaveData PlayerData      // 位置/旋转/生命/氧气
      ├── TArray<FItemSaveData> InventoryItems // 物品数据
      ├── FDateTime SaveTimestamp          // 存档时间戳
      └── FString SlotName                 // 槽位名
```

**数据持久化机制：**
- 各系统绑定 `OnSave` / `OnLoad` 委托以参与数据持久化
- `FOnSaveGame` / `FOnLoadGame` 为 `DECLARE_MULTICAST_DELEGATE_OneParam`，支持多播
- 存档数据结构见 [[SubSystems/SaveData.h]]

### UI 系统（骨架）

```
UGameInstanceSubsystem
 └── UUIManager                           // UI 状态管理
      └── void OpenOrCloseBag()           // 已实现：切换背包面板

UUserWidget
 ├── UMainMenu                           // 骨架（头文件存在，类声明完整）
 ├── UInventoryWidget                    // 骨架（Widget/Item/）
 └── UItemWidget                         // 骨架（Widget/Item/）
```

---

## 模块定义数据（DataAsset）

```cpp
UCLASS(BlueprintType)
class UModuleDef : public UDataAsset {
    // 标识
    FName ModuleID;
    FText DisplayName;
    EModuleCategory Category;    // Core/Propulsion/Survival/Functional/Defense/Weapon/Living
    
    // 网格
    FIntVector GridSize;         // 长×宽×高（以标准单元为单位）
    TArray<FConnectionPoint> ConnectionPoints;
    
    // 属性
    float Mass;
    float PowerConsumption;
    float DurabilityMax;
    
    // 功能
    TArray<FAbilityValue> Provides;
    // 例: {Ability: O2Generation, Value: 50.0}, {Ability: CargoSpace, Value: 200.0}
    int32 CrewCapacity;
    
    // 建造
    TMap<UItemDef*, int32> BuildCost;
    float BuildTime;             // 秒（0=即时）
    
    // 限制
    int32 MaxPerShip;            // -1=无限制
    EReactorClass RequiredReactor;  // Any/Light/Medium/Heavy
    EFaction Manufacturer;        // 影响外观风格
    
    // 外观
    UStaticMesh* Mesh;
    UMaterialInterface* DefaultMaterial;
};

USTRUCT()
struct FConnectionPoint {
    EConnectionType Type;        // Passage/Structural/Weapon/Pipe
    FVector LocalPosition;
    FVector Direction;
};
```

---

## 存档策略

```
SaveFile/
├── header.json              // 存档版本、时间戳、玩家名
├── player.dat               // 玩家运行时数据（二进制）
├── universe.dat             // 宇宙状态：已生成星系、已发现 POI
├── ships/                   // 每艘飞船独立文件
│   ├── ship_001.dat
│   └── ship_002.dat
├── bases/                   // 每个基地独立文件
│   ├── base_001.dat
│   └── base_002.dat
├── progression.dat          // 科技树、设计图、里程碑状态
├── narrative.dat            // 叙事线索、派系关系
└── starmap.dat              // 星图探索状态
```

**分层优势：** 建造数据独立 → 支持设计图导出/分享；死亡不回滚建造 → 只丢失出航收获；按需加载而非全量。

---

## 文档版本

| 版本 | 日期 | 变更 |
|------|------|------|
| v0.1 | 2026-06-01 | 初始框架，技术选型、8 大核心系统模块划分、关键数据结构、数据流设计 |
| v0.2 | 2026-06-01 | 重构为 UE5 类继承体系，新增完整继承树、每个系统的 UE 基类标注、接口伪代码 |
| v0.3 | 2026-06-12 | 设计文档与代码实现状态同步：02 号新增生存系统实现状态表；04 号电力系统新增实现引用；05 号物品系统实现状态细化；建造文档新增复杂度取舍与开发优先级；世界观文档新增通用AI芯片禁令设定 |
| v0.4 | 2026-06-13 | 代码同步更新：Build.cs 模块依赖补充 UMG/Slate/SlateCore；新增已实现系统（交互系统/UKeyManager/USaveManager/USaveData/ALsrShipPawn/ALsrGameMode/ALsrPlayerController）；标记 ABuildableActor/AInteriorFacilityActor 为已实现；AAppliance 更名为 AApplianceActor（继承 AInteriorFacilityActor）；UVitalAttributeSet 补充完整 4 属性；ACharacter 补充 IAbilitySystemInterface 实现 |
